# market-live skill

Guidance for OpenClaw agents to use `mcp-market-live` tools for current/latest market bar queries.

This skill intentionally excludes historical and backfill workflows, which stay in the `stocklake` skill.
