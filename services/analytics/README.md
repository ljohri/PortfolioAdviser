# analytics

`analytics` is a standalone service for derived insights, metrics, and analytical workloads.

## Scope (scaffold phase)

- Consume canonical data from datalake for analysis pipelines
- Produce computed artifacts and service-level outputs
- Avoid embedding screener logic to keep service separation clean

## Planned structure

- `tests/` for analytics pipeline and contract tests
- implementation modules to be added in later phases
